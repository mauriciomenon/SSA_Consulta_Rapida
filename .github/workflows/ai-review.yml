name: AI Review (Placeholder)

# Minimal placeholder workflow (no AI provider configuration present).
# Purpose: reserve name; safely expand later under governance.
#
# Activation guidance (do not commit sensitive details):
# 1. Decide triggers (push, pull_request) and remove this comment block when enabling.
# 2. Configure any external service via secrets/vars (never commit identifiers here).
# 3. Apply least-privilege permissions for any future write actions.
#
# SECURITY NOTE: No external calls performed; keep it this way until formally approved.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Placeholder notice
        run: |
          echo "AI review placeholder active. No external calls performed." \
           && echo "Add implementation steps when governance approves."
