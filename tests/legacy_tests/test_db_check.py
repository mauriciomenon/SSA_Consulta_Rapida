"""Legacy test placeholder: original depended on removed function `check_and_restore_database`.

Mantido apenas para referência histórica. Use a nova suíte moderna (fixtures + integrity checks).
"""
from __future__ import annotations

import pytest

pytest.skip("legacy placeholder (check_and_restore_database removido)", allow_module_level=True)
