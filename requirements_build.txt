# Compatibilidade de build; combinar com requirements.txt.
# Fluxo principal: uv sync --frozen --no-dev --extra build

pyinstaller>=6.22.2,<7
nuitka>=4.2.1,<5
pillow>=12.3.0,<13
cairosvg>=2.9.1,<3
pywin32>=311,<312; platform_system == 'Windows'
patchelf>=0.19.1.0,<1; platform_system == 'Linux'
