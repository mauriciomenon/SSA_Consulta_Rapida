# Streamlit Variant Design

File: `streamlit_app.py`

- Sidebar controls for range/years, timeout, SSL.
- Uses `utils.remote_itaipu` for backend fetch.
- Displays DataFrame and offers CSV download.

Run locally:
- `streamlit run streamlit_app.py`
