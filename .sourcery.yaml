version: '1'

# Simple configuration for SSA Consulta Rápida project
# Suggestions will appear only in Sourcery extension, not in Problems panel

ignore:
  - tests/legacy_tests/**
  - docs_saida/**
  - versoes_backup/**
  - __pycache__/**
