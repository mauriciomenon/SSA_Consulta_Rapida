
"source_path"str
±ß"
    L√™ um relat√≥rio Excel e retorna um DataFrame normalizado e metadados simples.

    Esta fun√ß√£o √© um inv√≥lucro (wrapper) em torno de `extract_data_from_excel`,
    mantendo compatibilidade com chamadas existentes que esperam uma tupla
    (df, meta).

    Args:
        file_path: Caminho do arquivo .xlsx a ser lido.

    Returns:
        Tuple[Optional[pd.DataFrame], Dict[str, Any]]: O DataFrame resultante (ou None em caso de erro)
        e um dicion√°rio de metadados m√≠nimos contendo ao menos o caminho de origem.
    "str	file_path
ı ı("∞F
D

Ñ	 Ñ(+df"-
extracao.extractorextract_data_from_excel:0
.

Ö Ö(9metadata"#new-object#Bdict"
 

Ö  Ö(8"	#map-set#:*
(

á á("#unknown-value#Btuple(
&

á á("#array-add-last#:(
&

á á(	"#array-add-last#:

á á(*<C:\Users\mauri\git\SSA_Consulta_Rapida\extracao\extractor.py